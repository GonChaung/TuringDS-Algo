package recursion;

import array.Array;
import array.BinarySearch;

public class BinarySearchRecursive extends Array {
    public BinarySearchRecursive(int[] items){
        super(items);
    }

    public int findItem( int value, int first, int last){
        int middle = (first + last) / 2;
        return -1;
    }
}
